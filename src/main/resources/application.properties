# Þetta allt er eins og Siggi gerði
#what to do on start
spring.jpa.hibernate.ddl-auto=update

#show the SQL commands in console for debugging
spring.jpa.show-sql=true

#What is the path to the DB
spring.datasource.url=jdbc:h2:file:./database/dbfile
spring.datasource.driverClassName=org.h2.Driver

#credentials
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

#enabling the H2 console
spring.h2.console.enabled=true





# Hér fyrir neðan er það sem ég var komin með

# spring.application.name=recipes

# # To use SQLite
# spring.application.name=sqlite
# spring.datasource.url=jdbc:sqlite:mydatabase.db
# spring.datasource.driver-class-name=org.sqlite.JDBC
# spring.jpa.show-sql=true
# spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
# spring.jpa.hibernate.ddl-auto=update
